node_modules/
dist/
*.tsbuildinfo
.DS_Store
results*.json
# The default --out name, so a run started from a checkout leaves nothing to commit.
safe-vanity-blockie-*.json
gallery*.html
.superpowers/
.next/
.vercel/
next-env.d.ts

# `next dev` auto-generates agent instruction files. They are untrusted generated content
# that instructs coding agents (including to commit themselves), so they stay out of git.
# Remove these lines if you deliberately want to adopt and review them.
AGENTS.md
CLAUDE.md
